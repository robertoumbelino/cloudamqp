A POC from cloudAMQP lib.

Needs to create `.env` file with `AMQP_URL` environment.

Servie to create AMQP_URL -> https://cloudamqp.com


```shell
yarn

yarn consumer:station
yarn consumer:customer-saver
yarn consumer:crm

yarn producer:station
```
